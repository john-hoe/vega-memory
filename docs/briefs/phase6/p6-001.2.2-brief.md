# P6-001.2.2 定义 used_sources / fallback_used / confidence 边界

- Wave: 6A Host-side Retrieval Workflow
- Group: A 宿主 retrieval 契约
- Priority: P1
- Value: 中
- Depends on: P6-001.2
- Type: spec

## Context

明确宿主能看到哪些 retrieval 结果信号，但不能把这些结果字段拿来替代 Vega 内部路由器。

## Acceptance Criteria

- **Artifact**: `docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md`
- **Command**: `rg -n "used_sources / fallback_used / confidence 边界|used_sources|fallback_used|confidence" docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md`
- **Assertion**: 命中至少 1 处，并明确三者的语义边界与宿主不可滥用的限制。
- **Output**: `文档固定 `used_sources`、`fallback_used` 与 `confidence` 的解释边界。`

## Steps

1. 定义 `used_sources` 只记录实际贡献 source
2. 定义 `fallback_used` 只表达是否降级而非内部所有分支
3. 定义 `confidence` 是 bundle-level 信号而不是新的 ranking engine

## Verification

```bash
test -f docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md && rg -n "used_sources / fallback_used / confidence 边界|used_sources|fallback_used|confidence" docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md
```
