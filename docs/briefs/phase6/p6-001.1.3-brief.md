# P6-001.1.3 定义 query_focus 与 host_hint 协议边界

- Wave: 6A Host-side Retrieval Workflow
- Group: A 宿主 retrieval 契约
- Priority: P1
- Value: 中
- Depends on: P6-001.1
- Type: spec

## Context

把 `query_focus` 和 `host_hint` 限定为 weak hints，避免宿主借此重新变厚。

## Acceptance Criteria

- **Artifact**: `docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md`
- **Command**: `rg -n "query_focus 与 host_hint 协议边界|history|docs|mixed|evidence" docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md`
- **Assertion**: 命中至少 1 处，并明确两者只能作为弱提示，不能直接指定 source / ranker。
- **Output**: `文档固定 `query_focus` 与 `host_hint` 的 advisory 边界。`

## Steps

1. 定义 `query_focus` 的 canonical 枚举
2. 列出 `host_hint` 允许承载的辅助上下文
3. 明确禁止把 hint 变成强制路由命令

## Verification

```bash
test -f docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md && rg -n "query_focus 与 host_hint 协议边界|history|docs|mixed|evidence" docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md
```
