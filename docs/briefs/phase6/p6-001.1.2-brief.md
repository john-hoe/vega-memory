# P6-001.1.2 定义 retrieval request 的 nullable 字段规则

- Wave: 6A Host-side Retrieval Workflow
- Group: A 宿主 retrieval 契约
- Priority: P1
- Value: 中
- Depends on: P6-001.1
- Type: spec

## Context

明确宿主在不知道上下文时应直接传 `null`，避免为了凑字段而编假值。

## Acceptance Criteria

- **Artifact**: `docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md`
- **Command**: `rg -n "nullable 字段规则|thread_id|project|cwd|host_hint" docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md`
- **Assertion**: 命中至少 1 处，并列出允许为空的字段集合和缺省处理原则。
- **Output**: `文档固定 retrieval request 的 nullable 规则。`

## Steps

1. 枚举允许 `null` 的字段
2. 写清“不知道就传 null，不编假值”的原则
3. 明确由 Vega 吸收缺省上下文而不是让宿主补假数据

## Verification

```bash
test -f docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md && rg -n "nullable 字段规则|thread_id|project|cwd|host_hint" docs/specs/2026-04-22-p6-001-host-retrieval-workflow-v1.md
```
