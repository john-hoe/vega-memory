# P5-002.2.4 设计 candidate record 完整字段 schema

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P0
- Value: 高
- Depends on: P5-002.1.1.1
- Type: spec

## Context

替代已跳过的 P5-002.2.1。

## Acceptance Criteria

- **Artifact**: `docs/specs/candidate-layer.md §字段 schema`
- **Command**: `grep -c 'event_id\|source_surface\|raw_payload_hash\|status\|created_at\|promoted_at' docs/specs/candidate-layer.md`
- **Assertion**: 返回数字 >= 6（6 个必填字段各出现一次）
- **Output**: `字段定义表含 name/type/required/description 4 列`

## Steps

1. 列出 candidate record 所有字段
2. 为每个字段标注类型和必填性
3. 写入 spec §字段 schema 章节

## Verification

```bash
test -f docs/specs/candidate-layer.md && echo PASS || echo FAIL
```
