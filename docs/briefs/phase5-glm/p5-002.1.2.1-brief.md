# P5-002.1.2.1 枚举 replay 触发场景并设计流程

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P1
- Value: 中
- Depends on: P5-002.1
- Type: spec

## Context

spec 文档 + 流程图。

## Acceptance Criteria

- **Artifact**: `docs/specs/raw-replay.md`
- **Command**: `grep -c 'schema 变更\|pipeline bug\|手动' docs/specs/raw-replay.md`
- **Assertion**: 返回数字 >= 3（3 个触发场景）
- **Output**: `文档含 3+ 个触发场景，每个含 replay 范围/隔离/进度追踪`

## Steps

1. 写 schema 变更触发场景
2. 写 pipeline bug 修复触发场景
3. 写手动 admin 触发场景
4. 画流程图

## Verification

```bash
test -f docs/specs/raw-replay.md && echo PASS || echo FAIL
```
