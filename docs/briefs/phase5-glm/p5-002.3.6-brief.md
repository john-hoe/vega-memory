# P5-002.3.6 定义 promoted export 与 audit 边界规则

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P2
- Value: 中
- Depends on: P5-002.3.4
- Type: spec

## Context

替代已跳过的 P5-002.3.3。

## Acceptance Criteria

- **Artifact**: `docs/specs/promoted-layer.md §export+audit`
- **Command**: `grep -c 'PII\|脱敏\|visibility.*变更\|audit.*log' docs/specs/promoted-layer.md`
- **Assertion**: 返回数字 >= 4
- **Output**: `export 格式规范 + audit 日志格式规范`

## Steps

1. 定义导出前的 PII 脱敏要求
2. 定义导出格式（JSON/CSV）
3. 定义 audit 日志格式（时间/操作/old/new）
4. 写入 spec

## Verification

```bash
test -f docs/specs/promoted-layer.md && echo PASS || echo FAIL
```
