# P5-003.3.1 设计人工复核与 override API

- Wave: 5C Retrieval & Promotion
- Group: C 检索与晋升
- Priority: P2
- Value: 中
- Depends on: P5-003.1.3.2
- Type: spec

## Context

人工可干预 promotion 结果：强制通过、拒绝、修改后重新派生。

## Acceptance Criteria

- **Artifact**: `docs/specs/manual-review-override.md`
- **Command**: `grep -c 'GET.*review\|POST.*approve\|POST.*reject\|POST.*override' docs/specs/manual-review-override.md`
- **Assertion**: 返回数字 >= 4（4 个 API 端点）
- **Output**: `OpenAPI spec 或 TypeScript 接口定义文件`

## Steps

1. 定义 review API 端点（list/approve/reject/edit）
2. 定义 override 端点（跳过 promotion 直接 promoted）
3. 定义权限模型
4. 输出 TypeScript 接口定义

## Verification

```bash
test -f docs/specs/manual-review-override.md && echo PASS || echo FAIL
```
