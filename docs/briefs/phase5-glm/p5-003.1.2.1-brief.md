# P5-003.1.2.1 定义 wiki/fact/insight 各类型的输入条件

- Wave: 5C Retrieval & Promotion
- Group: C 检索与晋升
- Priority: P2
- Value: 中
- Depends on: P5-003.1.1.1
- Type: spec

## Context

三类派生各有明确的准入门槛。

## Acceptance Criteria

- **Artifact**: `docs/specs/derived-pipeline.md §输入条件`
- **Command**: `grep -c 'wiki.*≥200\|fact.*可验证\|insight.*可操作' docs/specs/derived-pipeline.md`
- **Assertion**: 返回数字 >= 3（3 种类型各有准入条件）
- **Output**: `条件表含 类型/最小数据量/质量阈值/类型限制 4 列`

## Steps

1. 定义 wiki 输入条件（内容≥200字+主题一致）
2. 定义 fact 输入条件（可验证+有时间戳）
3. 定义 insight 输入条件（可操作+非显而易见）

## Verification

```bash
test -f docs/specs/derived-pipeline.md && echo PASS || echo FAIL
```
