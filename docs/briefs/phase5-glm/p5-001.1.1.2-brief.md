# P5-001.1.1.2 撰写必填/nullable 字段 spec 初稿

- Wave: 5A Host Event Envelope
- Group: A 宿主协议
- Priority: P0
- Value: 高
- Depends on: P5-001.1.1.1
- Type: spec

## Context

基于调研结果撰写，输出到 docs/specs/ 目录。

## Acceptance Criteria

- **Artifact**: `docs/specs/host-event-envelope-v1.md`
- **Command**: `grep -c '| required |' docs/specs/host-event-envelope-v1.md`
- **Assertion**: 返回数字 >= 10（至少 10 个必填字段行）
- **Output**: `Markdown 表格含 字段名/类型/必填/nullable条件/默认值/示例 6 列`

## Steps

1. 对比 host-fields.json vs 需要的字段
2. 为每个字段标注 required/nullable/default
3. 写入 spec §字段定义章节

## Verification

```bash
test -f docs/specs/host-event-envelope-v1.md && echo PASS || echo FAIL
```
