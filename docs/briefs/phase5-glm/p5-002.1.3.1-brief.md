# P5-002.1.3.1 定义 inbox/archive/audit 职责与权限模型

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P2
- Value: 中
- Depends on: P5-002.1
- Type: spec

## Context

权限模型可被代码直接实现。

## Acceptance Criteria

- **Artifact**: `docs/specs/raw-archive-audit.md`
- **Command**: `grep -c 'CRUD\|read.*append\|append.*only\|拒绝' docs/specs/raw-archive-audit.md`
- **Assertion**: 返回数字 >= 4
- **Output**: `权限矩阵 Markdown 表格（inbox/archive/audit × 操作）`

## Steps

1. 定义 inbox=CRUD 权限
2. 定义 archive=read+append 权限
3. 定义 audit=append-only 权限
4. 列出越权操作和错误码

## Verification

```bash
test -f docs/specs/raw-archive-audit.md && echo PASS || echo FAIL
```
