# P5-002.3.4 设计 promoted record schema 与 visibility scope

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P0
- Value: 高
- Depends on: P5-002.2.5
- Type: spec

## Context

替代已跳过的 P5-002.3.1。

## Acceptance Criteria

- **Artifact**: `docs/specs/promoted-layer.md §字段 schema`
- **Command**: `grep -c 'visibility\|quality_score\|derived_from' docs/specs/promoted-layer.md`
- **Assertion**: 返回数字 >= 3（3 个 promoted 专有字段各出现一次）
- **Output**: `字段定义表含 candidate 全部字段 + visibility/quality_score/derived_from`

## Steps

1. 在 candidate 字段基础上增加 promoted 专有字段
2. 定义 visibility 枚举（project/global/private）
3. 写入 spec

## Verification

```bash
test -f docs/specs/promoted-layer.md && echo PASS || echo FAIL
```
