# P5-001.1.2.1 收集所有已知 surface 和 event_type 值

- Wave: 5A Host Event Envelope
- Group: A 宿主协议
- Priority: P0
- Value: 高
- Depends on: P5-001.1
- Type: research

## Context

输出为 JSON 或 YAML 格式的 registry 初始数据。

## Acceptance Criteria

- **Artifact**: `docs/specs/canonical-values.json`
- **Command**: `jq '.surfaces | length' docs/specs/canonical-values.json && jq '.event_types | length' docs/specs/canonical-values.json`
- **Assertion**: surfaces >= 4 且 event_types >= 10
- **Output**: `JSON 文件含 surfaces[] 和 event_types[] 两个数组`

## Steps

1. 从四个宿主源码提取 surface 和 event_type 字符串
2. 去重并标注 aliases 和 description
3. 输出为 canonical-values.json

## Verification

```bash
test -f docs/specs/canonical-values.json && echo PASS || echo FAIL
```
