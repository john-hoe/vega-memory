# P5-001.2.3.1 撰写投递可靠性 spec（幂等/重试/离线/补发）

- Wave: 5A Host Event Envelope
- Group: A 宿主协议
- Priority: P1
- Value: 高
- Depends on: P5-001.2
- Type: spec

## Context

可操作性 spec，开发者可直接实现。

## Acceptance Criteria

- **Artifact**: `docs/specs/delivery-reliability.md`
- **Command**: `grep -c 'event_id\|retry.*1s\|cache.*1000\|order' docs/specs/delivery-reliability.md`
- **Assertion**: 返回数字 >= 4
- **Output**: `文档含幂等去重/重试参数/离线缓存/补发顺序 4 个维度各带参数表`

## Steps

1. 写 event_id 去重规则
2. 写重试参数（初始 1s/最大 60s/3 次）
3. 写离线缓存上限 1000 条
4. 写补发顺序保证

## Verification

```bash
test -f docs/specs/delivery-reliability.md && echo PASS || echo FAIL
```
