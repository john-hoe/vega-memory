# P5-003.1.1.2 实现晋升评分函数

- Wave: 5C Retrieval & Promotion
- Group: C 检索与晋升
- Priority: P1
- Value: 高
- Depends on: P5-003.1.1.1
- Type: implementation

## Context

代码实现 + 评分逻辑测试。

## Acceptance Criteria

- **Artifact**: `src/promotion/calculatePromotionScore.ts`
- **Command**: `node -e "const {calculatePromotionScore} = await import('./dist/promotion/calculatePromotionScore.js'); const s = calculatePromotionScore({frequency:0.9,crossSession:true,explicit:false,quality:0.8}); console.log(typeof s, s >= 0, s <= 1);" 2>/dev/null`
- **Assertion**: 输出 'number true true'（返回 0-1 浮点数）
- **Output**: `calculatePromotionScore() 函数导出`

## Steps

1. 实现评分计算逻辑
2. 输入为信号对象，输出 0-1 浮点数
3. 超过阈值 T 返回 promotable 标记

## Verification

```bash
npm run build && npm test
```
