# P5-002.3.5 定义 promoted retention 与 decay 配置规则

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P1
- Value: 中
- Depends on: P5-002.3.4
- Type: spec

## Context

替代已跳过的 P5-002.3.2。

## Acceptance Criteria

- **Artifact**: `docs/specs/promoted-layer.md §retention+decay`
- **Command**: `grep -c 'decay\|quality_score\|access_count\|VEGA_' docs/specs/promoted-layer.md`
- **Assertion**: 返回数字 >= 4
- **Output**: `decay 计算公式和默认参数，env 配置项名称`

## Steps

1. 定义基于 quality_score 和 access_count 的 decay 公式
2. 定义默认参数
3. 定义 env 配置项名
4. 写入 spec

## Verification

```bash
test -f docs/specs/promoted-layer.md && echo PASS || echo FAIL
```
