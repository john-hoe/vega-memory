# P5-002.1.1.1 定义 raw inbox 热/温/冷分层参数

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P0
- Value: 高
- Depends on: P5-002.1
- Type: spec

## Context

每层有明确的 TTL、存储位置和访问性能特征。

## Acceptance Criteria

- **Artifact**: `docs/specs/raw-inbox-retention.md §分层参数`
- **Command**: `grep -c '热\|温\|冷\|删除' docs/specs/raw-inbox-retention.md`
- **Assertion**: 返回数字 >= 8（热/温/冷/删除各出现至少 2 次）
- **Output**: `Markdown 表格 4 行（热/温/冷/删除）× 4 列（层级/TTL/存储后端/读写性能）`

## Steps

1. 定义热层（7天/SQLite WAL）
2. 定义温层（30天/归档）
3. 定义冷层（90天/文件）
4. 定义删除（>90天）

## Verification

```bash
test -f docs/specs/raw-inbox-retention.md && echo PASS || echo FAIL
```
