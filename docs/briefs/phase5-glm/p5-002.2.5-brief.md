# P5-002.2.5 设计 candidate lifecycle 状态机

- Wave: 5B Vega Layered Ingestion
- Group: B 分层摄取
- Priority: P0
- Value: 高
- Depends on: P5-002.2.4
- Type: spec

## Context

替代已跳过的 P5-002.2.2。状态转换图写入 spec。

## Acceptance Criteria

- **Artifact**: `docs/specs/candidate-layer.md §状态机`
- **Command**: `grep -A 20 'stateDiagram-v2' docs/specs/candidate-layer.md | grep -c 'new\|evaluating\|promoted\|rejected\|deduped'`
- **Assertion**: 返回数字 >= 5（5 个状态名各出现一次）
- **Output**: `Mermaid stateDiagram-v2 含 5 个状态和 6+ 条转换边`

## Steps

1. 定义 new/evaluating/promoted/rejected/deduped 5 个状态
2. 定义每个转换的触发条件
3. 画 Mermaid 状态转换图
4. 写入 spec

## Verification

```bash
test -f docs/specs/candidate-layer.md && echo PASS || echo FAIL
```
