# P5-003.2.1 设计 promotion → retrieval 反馈信号模型

- Wave: 5C Retrieval & Promotion
- Group: C 检索与晋升
- Priority: P1
- Value: 中
- Depends on: P5-003.1.1.2
- Type: spec

## Context

当 promoted 内容被频繁访问或标记为低质量时，retrieval 应调整其排序权重。

## Acceptance Criteria

- **Artifact**: `docs/specs/promotion-retrieval-feedback.md`
- **Command**: `grep -c 'access_count\|quality_score\|retrieval.*权重\|信号名' docs/specs/promotion-retrieval-feedback.md`
- **Assertion**: 返回数字 >= 3
- **Output**: `反馈信号表含 信号名/触发条件/影响权重 3 列`

## Steps

1. 定义 promoted 内容访问→权重上调信号
2. 定义低质量标记→权重下调信号
3. 定义反馈写入时机
4. 写入 spec

## Verification

```bash
test -f docs/specs/promotion-retrieval-feedback.md && echo PASS || echo FAIL
```
