# P5-003.2.2 实现 promotion feedback 写入与 retrieval 权重更新

- Wave: 5C Retrieval & Promotion
- Group: C 检索与晋升
- Priority: P2
- Value: 中
- Depends on: P5-003.2.1
- Type: implementation

## Context

代码实现反馈闭环。

## Acceptance Criteria

- **Artifact**: `src/promotion/promotionFeedback.ts + src/tests/promotionFeedback.test.ts`
- **Command**: `npm test -- promotionFeedback`
- **Assertion**: 访问 promoted 后 retrieval 权重上调（delta > 0）；标记低质量后权重下调（delta < 0）
- **Output**: `promotionFeedback 模块导出 writeFeedback() 和 updateRetrievalWeight()`

## Steps

1. 实现 writeFeedback() 记录反馈信号
2. 实现 updateRetrievalWeight() 更新权重
3. 写测试

## Verification

```bash
npm run build && npm test
```
