# P5-003.1.1.1 设计晋升信号模型（频率/跨 session/显式标记/聚合）

- Wave: 5C Retrieval & Promotion
- Group: C 检索与晋升
- Priority: P1
- Value: 高
- Depends on: P5-002.3.4
- Type: spec

## Context

spec 文档含公式和阈值建议。

## Acceptance Criteria

- **Artifact**: `docs/specs/promotion-signals.md §信号模型`
- **Command**: `grep -c '频率\|session\|显式\|聚合\|w[1-4]\|阈值' docs/specs/promotion-signals.md`
- **Assertion**: 返回数字 >= 6
- **Output**: `公式 Σ(wi×si) 和 3 个计算示例写入文档`

## Steps

1. 定义 4 类信号（频率/跨session/显式标记/聚合质量分）
2. 定义权重 w1-w4
3. 定义总分公式和阈值 T
4. 写 3 个计算示例

## Verification

```bash
test -f docs/specs/promotion-signals.md && echo PASS || echo FAIL
```
