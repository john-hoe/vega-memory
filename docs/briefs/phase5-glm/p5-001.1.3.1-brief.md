# P5-001.1.3.1 撰写 schema version 演进原则文档

- Wave: 5A Host Event Envelope
- Group: A 宿主协议
- Priority: P2
- Value: 中
- Depends on: P5-001.1.1.2
- Type: spec

## Context

为后续 v1→v2 迁移提供规范基础。

## Acceptance Criteria

- **Artifact**: `docs/specs/schema-versioning.md`
- **Command**: `grep -c '## \|### ' docs/specs/schema-versioning.md`
- **Assertion**: 返回数字 >= 4（至少 4 个章节标题）
- **Output**: `文档含 semver 规则、向前兼容原则、deprecation 流程、v1→v2 迁移示例`

## Steps

1. 写版本号规则（semver）
2. 写向前兼容原则（新字段必须有默认值）
3. 写 deprecation 流程（≥2 版本后移除）
4. 写 v1→v2 迁移示例

## Verification

```bash
test -f docs/specs/schema-versioning.md && echo PASS || echo FAIL
```
