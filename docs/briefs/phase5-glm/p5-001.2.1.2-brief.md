# P5-001.2.1.2 实现宿主侧 redaction 函数

- Wave: 5A Host Event Envelope
- Group: A 宿主协议
- Priority: P0
- Value: 高
- Depends on: P5-001.2.1.1
- Type: implementation

## Context

TypeScript 实现 + 性能基准测试。

## Acceptance Criteria

- **Artifact**: `src/host/redactPayload.ts`
- **Command**: `node -e "const {redactPayload} = await import('./dist/host/redactPayload.js'); const r = redactPayload({a:'sk-abc123'}); console.log(r.a);" 2>/dev/null | grep -c REDACTED`
- **Assertion**: 返回 1（sk-abc123 被脱敏为含 REDACTED 的字符串）
- **Output**: `导出 redactPayload() 函数`

## Steps

1. 加载 redaction-rules.json
2. 递归扫描 JSON 匹配规则
3. 替换为 ***REDACTED***
4. 编译通过

## Verification

```bash
npm run build && npm test
```
