# P5-001.2.2.2 设计 artifacts 外部化 schema

- Wave: 5A Host Event Envelope
- Group: A 宿主协议
- Priority: P1
- Value: 中
- Depends on: P5-001.2.2.1
- Type: spec

## Context

与 truncation 配合，大内容先外部化再截断。

## Acceptance Criteria

- **Artifact**: `docs/specs/artifacts-schema.json`
- **Command**: `npx ajv validate -s docs/specs/artifacts-schema.json -d '{"url":"https://x","mime_type":"text/plain","sha256":"abc","size_bytes":100,"expires_at":"2026-01-01"}' 2>&1 || echo 'ajv not available, manual check'`
- **Assertion**: schema 含 url/mime_type/sha256/size_bytes/expires_at 5 个必填字段
- **Output**: `JSON Schema 文件`

## Steps

1. 定义 artifacts 数组的 JSON Schema
2. 每项含 5 个必填字段
3. 用 ajv 或 zod 验证

## Verification

```bash
test -f docs/specs/artifacts-schema.json && echo PASS || echo FAIL
```
