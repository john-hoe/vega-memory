# P7-001.2.3 定义 needs_external 判定样例与边界

- Wave: 7A Host-side Usage Workflow
- Group: 宿主 workflow
- Priority: P1
- Value: 高
- Depends on: P7-001.2
- Type: spec

## Context

`needs_external` 是把控制权移交给 fallback ladder 的 handoff 点，必须明确哪些缺口已经超出 Vega。

## Acceptance Criteria

- **Artifact**: `docs/specs/2026-04-23-p7-001-host-side-usage-workflow-v1.md`
- **Command**: `rg -n "needs_external 判定样例与边界|本地 workspace|外部来源" docs/specs/2026-04-23-p7-001-host-side-usage-workflow-v1.md`
- **Assertion**: 文档明确本地/外部缺口的典型样例，以及何时必须交给 fallback ladder。
- **Output**: `needs_external handoff 边界固定。`

## Steps

1. 列出 needs_external 的最低条件
2. 写清本地 repo / 日志 / 环境 / 官方文档等典型样例
3. 明确这不是失败，而是进入 fallback 的 handoff

## Verification

```bash
test -f docs/specs/2026-04-23-p7-001-host-side-usage-workflow-v1.md && \
rg -n "needs_external 判定样例与边界|本地 workspace|外部来源" docs/specs/2026-04-23-p7-001-host-side-usage-workflow-v1.md
```
